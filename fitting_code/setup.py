from setuptools import setup, find_packages

setup(
    name="Limb Fitting Titan's Atmosphere",
    version='0.1',
    packages=find_packages(),
    # Other metadata like author, description, dependencies, etc.
)
